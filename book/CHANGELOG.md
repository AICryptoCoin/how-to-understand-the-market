# Журнал изменений — «Как понимать рынок»

История редакций книги. Записи новее — выше. Восстановлена из `git log` по
каталогу `docs/knowledge/market-cycle/`.

**Что здесь есть, а чего нет.** Здесь — изменения книги как издания: новые главы,
смена правил вёрстки и работы с источниками, сквозные починки. Точечные
исправления фактов, чисел и формулировок внутри уже вышедших глав — не здесь,
а на странице [errata.html](errata.html): у них своя таблица «что было → что
стало → почему».

**Нумерация.** `X.Y.Z`. Мажор — смена замысла или структуры книги. Минор —
новые главы или смена правил, по которым главы пишутся. Патч — сквозная
починка вёрстки без изменения содержания. Текущая редакция — **2.4.1**.

**Номера глав в записях.** Записи редакций 1.x называют главы по нумерации
первой редакции (24 главы). В редакции 2.0 книга состоит из 71 главы, и 20 глав
из 24 сменили номер. Старые записи не перенумерованы: они датированы и парны
строкам [errata.html](errata.html). Таблица соответствия —
`../FULL-BOOK-PLAN.md` §2.

**Кем.** Все коммиты сделаны сессиями Claude Code под git-пользователем
репозитория `AI Crypto`. Отдельные сессии писали главы параллельно, поэтому
порядок коммитов не совпадает с порядком глав в книге.

---

## 2.4.1 — 2026-08-02 · семь коллизий на узком крае и пол размера шрифта · AI Crypto

Починка вёрстки без изменения содержания. Волна `2.4` сдала главу 14 с записью
о найденном, но не почищенном дефекте: канонический прибор `STYLE-GUIDE` §7
зажигал на ширине **320 px** семь коллизий подписей — три в рис. 14.1 и четыре
в рис. 14.4, все семь пары соседних строк одной двухстрочной аннотации. Дефект
подтверждён как **не внесённый той волной**: версия из коммита до правки даёт
те же семь пар, а SVG-блоки обеих версий побайтово одинаковы.

**Причина оказалась не той, что стояла в записи `2.4`.** Там сказано, что текст
масштабируется вместе с SVG и потому «становится примерно в 3,6 раза выше».
Замер даёт другое. `.viz-label` — **12px, не 13px**, а высота глифовой рамки
в единицах viewBox не линейна по масштабу, а **квантована**:

| ширина окна | масштаб `getScreenCTM().a` | 12 единиц × масштаб | высота рамки, единиц viewBox | перекрытие при шаге 15 |
|---|---|---|---|---|
| 320 px | 0,2767 | 3,32 px | **18,07** | **+3,07** — прибор зажигает (порог > 3) |
| 375 px | 0,3407 | 4,09 px | 14,68 | −0,32 — чисто |

Браузер **не рисует текст мельче ≈ 4,09 px**, и ниже этой границы кегль
перестаёт уменьшаться вместе с фигурой. Обратный пересчёт в единицы viewBox
раздувает рамку с 14,68 до 18,07 — при шаге строк 15 это перекрытие 3,07
при пороге 3. **Порог брался с перевесом 0,07 единицы**, ровно как у рис. 8.4
в `d7e6f06` (там тоже 3,07). Отсюда и то, почему ломается только 320 px:
граница пола приходится на масштаб 0,3407, то есть ровно на 375 px, и все
ширины выше него дают неизменные 14,68.

**Починка.** Шаг внутри аннотаций разведён **15 → 19 единиц** в обеих фигурах,
каждая пара заново отцентрована по своей скобке. Девятнадцать выбраны
не на глаз: 18,07 − 19 = −0,93, то есть до порога остаётся **3,93 единицы**
в худшей из шести измеренных ширин. Изменены **только координаты `y`
одиннадцати подписей** — ни слова, ни числа, ни `desc`, ни табличные двойники
не тронуты; порог прибора не двигали.

### Приёмка

Мерено в настоящем браузере на поднятом `http.server`; что сервер живой,
доказано запросом несуществующего файла — **404, а не 200**. Шесть ширин
320 / 375 / 768 / 1024 / 1280 / 1600 px, каждая — с явным `resize_window`
**и повторной навигацией**, с проверкой перед замером: `clientWidth` ненулевой,
`document.fonts.status === 'loaded'`, масштаб `getScreenCTM()` не равен ровно
единице. На всех шести: `overflow: []`, `collisions: []`, `htmlInSvg: 0`,
`pageScroll: false`; консоль чистая. Что прибор жив, доказано **мутацией**:
в рис. 14.4 поверх «Норма» временно подсаживалась строка — прибор поймал
коллизию и назвал мутанта, после отката снова чисто.

**Измерение через `iframe` не применялось.** Запрет введён волной `2.4`
и подтверждается здесь: во вложенном контексте колонка книги схлопывается
при верном `clientWidth`, и прибор выдаёт коллизии, которых на настоящей
странице нет.

`linkify.py --check` — 40 страниц, ноль дефектов; `audit.py` — ноль по всем
девяти детекторам, `--selftest` зелёный.

**Что осталось нетронутым и почему.** В рис. 14.3 две пары строк идут с шагом
18 и дают на 320 px зазор **0,07 единицы** — прибор их пропускает (порог 3),
в списке дефекта их не было, и эта починка их не касалась. Запас там втрое
меньше, чем в двух починенных фигурах.

---

## 2.4 — 2026-08-02 · две задачи контура внесены в книгу: `Z06` и `Z25` · AI Crypto

Новых глав нет. Волна вносит в уже вышедшие главы, в спецификацию и в каталог
индикаторов результаты двух задач контура `research/`, и это первая волна,
у которой **главная работа состояла в том, чтобы не переписать лишнего**.

### Что именно внесено, и чего внесено не было

`Z06` строила композит-заменитель ISM из пяти региональных обзоров ФРБ и пыталась
откалибровать его собственную пороговую линию. Вердикт — **ОПРОВЕРГНУТО**: линия
нулевого роста не идентифицируется (оценка на 1,96 сигмы ниже среднего, ниже неё
8 месяцев из 371, интервал шириной 4,07 сигмы при требуемой менее одной), связь
рвётся в конце 2009 года, порядок четырёх ступеней инвертируется. При этом
композит распознаёт рецессию с площадью под кривой **0,9155**.

`Z25` проверяла, чьё свойство перелом 2009 года. Вердикт по объявленному правилу —
**НЕОПРЕДЕЛЕНО**, но числа однозначнее вердикта: связь с квартальным ростом ВВП
после 2009Q4 потеряли **все двадцать** проверенных рядов, и опросные, и жёсткие;
две пятых разрыва создано тем, что в после-режиме нет ни одной рецессии.

**Главное решение волны — отрицательное.** Числа курса **48.7 / 49.9 / 52.7 /
43–44 не вычеркнуты ни в одном из 30 мест**, где книга их называет. Свободного
ряда ISM не существует, `Z06` их не проверяла и прямо записала, что они
«остаются НЕПРОВЕРЕННЫМИ и таковыми останутся». Опровергнут **перенос
конструкции** «пороговая линия уровня» на открытый заменитель. Соблазн вычеркнуть
четыре числа как опровергнутые был бы ошибкой того же класса, что и сама
опровергнутая конструкция: подменой одного утверждения похожим. Поэтому правка
почти везде одна и та же — **число курса остаётся, рядом встаёт измеренное
и то, что именно проверить не удалось.**

### Где правлено

**Спецификация** — §1.4.1 (четырёхступенчатая шкала остаётся; рядом двухступенчатая
для заменителя, порог −0,234 сигмы), §1.4.4 (ярус 1: чему он больше не обучается;
ни один вес не пересмотрен), §1.4.7 (осечка `CFNAI`), §2, §7-B (`R10`, `R11`),
журнал — пятый заход.

**Каталог индикаторов** — новая врезка **§0.3**, единственное место каталога, где
сказано, что стало с пороговыми линиями; карточки 2.1 / 2.2 / 2.3, таблица
breakeven-линий, свод правил блока, сводная фазовая таблица и **ограничение §G
п. 1** ссылаются на неё и своих чисел не заводят. Устройство повторяет §0.2,
куда тем же способом сведён результат `Z02`.

**Главы** — 14 (две врезки: попытка построить линию заново и проверка самой
регрессии; подписи обеих фигур, три карточки, свод в конце), 3 (врезка к абзацу
«обучаем быстрые по медленной» — прямому адресату результата; упражнение
чек-листа переписано), 9 (врезка «чей это перелом»; оба решения, оставленные
главой архитектору, записаны как принятые), 59, 12, 70, 71, 4.

**Карта пробелов** — пункт 19 «обновление всех числовых порогов» помечен как
частично закрытый с объяснением, почему остаток невыполним в принципе.

### Что найдено сверх списков отчётов

Оба отчёта честно предупреждали, что их списки собраны поиском по числам
и формулировки без чисел в них не попали. Собственный обход нашёл четыре вещи,
которых в списках не было, и две из них — **образцы-размножители**:

1. **`indicator-catalog.md` §G п. 1** — перечень «у кого есть явные пороги»,
   парный пункту 2 того же раздела, куда волна `Z01` вносила лиды. Число оттуда
   копировалось бы в каждую новую карточку.
2. **`coverage-and-gaps.md` п. 19** — живой список задач «пересчитать пороги»,
   на который ни один из отчётов не ссылался.
3. **Глава 14, объяснение отказа 2023–2025 годов** структурным сдвигом («доля
   обрабатывающей промышленности сократилась») — механизм без единого числа,
   и потому в поиск он не попал. `Z25` его **измерила и отвергла**: композит
   теряет связь и с промышленным производством, то есть со своим собственным
   сектором.
4. **`razryv-vypuska.html`** уже нёс результат `Z06` целиком — эта глава вышла
   после задачи. Волна не дублировала её, а дописала то, чего в ней не могло
   быть: результат `Z25` и запись о том, что оба оставленных архитектору решения
   приняты.

### Отчёты доработаны в день внесения — числа сверены заново

Пока волна шла, оба отчёта контура были доработаны: `Z25` добрала ярус 1
(`PERMIT` и Philly Fed `NOF` — треть веса яруса, прежде неизмеренная),
досчитала ковид-включённую ветвь и исправила две формулировки; `Z06` закрыла
дыру, из-за которой забракованная лестница считалась молча. **Вердикты
не менялись, и ни одно пред-регистрированное число не сдвинулось** — прогон
воспроизвёл прежнюю стенограмму построчно.

Все **39 чисел**, внесённых этой волной в книгу, спецификацию и каталог, сверены
с доработанными отчётами и совпали до последнего знака. Добор яруса вывод
не ломает, а укрепляет: `PERMIT` — жёсткая половина жилищной пары — даёт после
2009 года корреляцию **+0,045**, ровно там же, где первичные заявки (+0,043),
композит заказов (+0,040) и занятость (+0,039). Две формулировки, устаревшие
от добора, поправлены в спецификации.

### Приёмка

`linkify.py --check` — 40 страниц без дефектов; `audit.py` — ноль дефектов
по девяти детекторам, `--selftest` зелёный.

**Геометрия мерена в настоящем браузере** на поднятом `http.server`; что сервер
живой, доказано запросом несуществующего файла — **404, а не 200**. **54 замера:**
девять тронутых страниц × шесть ширин 320 / 375 / 768 / 1024 / 1280 / 1600 px.
Перед каждым замером проверялись предусловия: `documentElement.clientWidth`
ненулевой, `document.fonts.status === 'loaded'`, масштаб `getScreenCTM()`
не равен ровно единице. Что прибор жив, доказано **мутацией**: в фигуру временно
подсаживался текст поверх существующего — прибор поймал коллизию и назвал
мутанта, после отката снова чисто.

**Отдельно — про способ измерения, потому что он едва не подделал результат.**
Первая попытка мерила страницы во вложенных `iframe`, чтобы обойтись меньшим
числом обращений к браузеру. Способ оказался **негодным**: в дочернем контексте
раскладка книги схлопывается — при верном `clientWidth` = 1265 колонка контента
сжимается с 961 px до 304 px, фигура рендерится втрое меньше, а размер шрифта
подписей задан в абсолютных пикселях и потому не масштабируется. Прибор
исправно сообщал коллизии, которых на настоящей странице нет. **Все 54
`iframe`-замера выброшены**, измерение переделано в верхнеуровневом документе,
а сам метод проверен на совпадение с настоящей навигацией: `clientWidth`,
масштаб `getScreenCTM()` и все четыре метрики совпали до знака.

**Найден один дефект, и он не наш.** На ширине 320 px в главе 14 прибор находит
**7 коллизий** подписей внутри фигур 1 и 4 — соседние строки многострочных
аннотаций наезжают друг на друга, потому что шрифт задан в абсолютных пикселях,
а SVG на узком экране масштабируется до 0,28. Проверено, что дефект **был там
до волны**: версия из коммита до правки даёт те же 7 пар, а SVG-блоки обеих
версий побайтово одинаковы — волна правила только прозу. Класс тот же, что
у пяти коллизий, исправленных в `d7e6f06`. Починка вынесена отдельной задачей:
она требует правки геометрии фигур, которых эта волна не касалась.

---

## 2.3 — 2026-08-01 · часть III закрыта: AS–AD и разрыв выпуска · AI Crypto

Две новые главы — вся часть «Спрос и разрыв» целиком, две главы из двух.
Часть II объяснила, откуда берётся потолок; часть III строит плоскость,
на которой этот потолок стоит вертикалью, и разбирается с тем, что расстояние
до него **не наблюдается ни одним прибором**.

**Написано 38 глав из 71**, 330 862 слова прозы, 198 фигур.

### Глава «Совокупный спрос и совокупное предложение» — 10 770 слов, 7 фигур

Плоскость, на которой одновременно видны цена и объём, и предупреждение первой
строкой: по вертикали отложен **уровень цен, а не темп**, поэтому однократный
сдвиг кривой даёт всплеск инфляции, который заканчивается сам. Кривая спроса
и сумма расходов — разные вещи, и компонентный разбор релиза о выпуске
отвечает на вопрос «куда пошёл прирост», а не «откуда он взялся». Четыре
механизма наклона совокупного спроса, из которых три количественно слабы,
а два описывают денежную систему, которой больше нет; работающий четвёртый —
реакция центрального банка, откуда **наклон кривой спроса оказывается параметром
политики, а не законом природы**, с тремя названными режимами отказа. Три
несовместимых объяснения наклона краткосрочного предложения и то, что каждое
предсказывает сверх наклона. Вертикаль на потенциале как утверждение, а не
определение. Четыре базовых случая с предписанием политике для каждого; переход
к плоскости «инфляция — разрыв» и цена этого перехода. Дерево из трёх вопросов,
превращающее «смотреть сквозь шок предложения» из житейского правила в вывод
с посылками. Почему спад обычно дезинфляционен, а стагфляция — исключение,
и что это говорит о происхождении любого исторического правила.

**Центральный узел — предел признака.** Совместный знак цены и объёма — это
единственный формальный признак, отличающий шок предложения от шока спроса,
и глава выводит его из рамки, а не постулирует. Тут же назван его предел:
при **одновременном** сдвиге обеих кривых пара «цена — объём» перестаёт
различать источники, и отказ этот арифметический, а не измерительный —
двух наблюдаемых величин не хватает на два одновременных источника. Хуже
всего то, что признак не сообщает об отказе: он молчит, а молчание выглядит
как уверенный ответ «двигался спрос».

**Что глава добавляет к инструменту.** Ни одного веса и ни одного порога —
они в спецификации, §1.4. Четыре обоснования, ни одного решения: признак
совместного знака обязан быть **флагом рядом с осью, а не слагаемым свёртки**
(довод арифметический — сумма не умеет читать совместный знак двух величин);
это уже **третье предложение флага подряд** от трёх разных глав, и потому
вопрос стоит не про очередной вход, а про существование целого слоя
квалифицирующих признаков; в трёх режимах отказа наклона обе оси инструмента
теряют независимость; и формулировка «состояние лучше» недоопределена, пока
не сказано, лучше по объёму или по цене.

### Глава «Потенциальный выпуск и разрыв выпуска» — 10 317 слов, 8 фигур

Три определения потенциала, которые не совпадают ни численно, ни по знаку
разрыва относительно каждого. Различение уровня потенциала и **темпа его
роста**: ошибка в уровне остаётся ошибкой, ошибка в темпе накапливается,
и именно так небольшое расхождение превращается в большое. Три способа оценки
и колонка «что задаётся руками» для каждого — во всех трёх ручных решений
столько же или больше, чем оцениваемых величин. Почему фильтр порождает
циклическую структуру в рядах, где никакого цикла нет. Проблема конца выборки
и три её следствия, из которых третье — предсказуемость знака пересмотра.
Полуструктурные модели и то, что разрыв выпуска и естественная ставка
оцениваются **одной процедурой одновременно**, а значит не являются
независимыми свидетельствами друг о друге. Гистерезис как позиция и возражение
о том, что та же картина получается от одного лишь устройства процедуры оценки.
Разрыв безработицы как замена, которая улучшает уменьшаемое и не трогает
вычитаемое; отношение вакансий к безработным как замена, устраняющая
ненаблюдаемый параметр ценой ненаблюдаемого **вида функции**.

**Единственный измеренный нами результат главы — отрицательный, и он
содержательнее числа.** Пороговая линия, определённая через нулевой рост
выпуска, на композите пяти региональных обзоров **не идентифицируется**:
её оценка лежит на 1,96 стандартного отклонения ниже среднего, ниже неё
8 месяцев из 371, а девяностопроцентный интервал имеет ширину 4,07 сигмы —
вчетверо больше самого стандартного отклонения. Связь при этом неустойчива:
после конца 2009 года наклон падает с 2,44 до 0,087, корреляция — с +0,873
до +0,040. Отказал **не композит, а перенос на него конструкции «пороговая
линия уровня»**: рецессию тот же композит распознаёт с площадью под кривой
0,9155, а линия рецессии калибруется с интервалом 0,43 сигмы. Отсюда общий
механизм: **всякий порог вида «ниже такого-то уровня начинается сжатие» —
замаскированный порог на разрыве выпуска**, и потому он наследует дрейф
эталона. Три встречи книги с этим механизмом (лид опережающего индикатора,
частота ложных тревог инверсии, пороговая линия активности) сведены рядом:
общее у них не то, что числа неверны, а то, что они предъявлялись без интервала
и без даты.

**Что глава добавляет к инструменту.** Ни одного веса и ни одного порога.
Четыре обоснования и четыре решения, оставленные спецификации: чем заменить
четырёхступенчатую шкалу уровня, которая на замене не воспроизводится
(порядок ступеней нарушен — «линия нулевого роста» оказалась ниже линии
рецессии); как **переименовать** оставшуюся линию, чтобы в спецификацию
не уехало число с чужой подписью; в какой форме хранить пороги, привязанные
к ненаблюдаемому эталону, — с датой сверки и интервалом, а не константой;
и требование винтажа **оценки на дату**, а не только данных на дату, для любого
ненаблюдаемого входа. Отдельно названо, чего глава не предлагает: выбрасывать
разрыв выпуска нельзя — им пользуется тот, чьё поведение читатель предсказывает.

### Приёмка

`audit.py` — ноль дефектов по всем девяти детекторам, `--selftest` зелёный,
`linkify.py --check` — 40 страниц без дефектов.

**Геометрия мерена в настоящем браузере** на поднятом `http.server`; что сервер
живой, доказано запросом несуществующего файла — **404, а не 200**. Предусловия
проверялись перед каждым замером и записывались: `documentElement.clientWidth`
ненулевой, `document.fonts.status === 'loaded'`, `getScreenCTM()` невырожденная.
Сводка: **24 замера** (две главы × двенадцать ширин 320–1600 px),
**3972 измеренных текстовых узла**, ноль переполнений, ноль коллизий, ноль
`htmlInSvg`, ноль горизонтальных скроллов, ноль случаев слепого прибора.

**Прибор проверен на живость** — иначе пустой результат неотличим от сломанного
детектора. Геометрия испорчена намеренно **в памяти** (подпись вытолкнута
за `viewBox`, две подписи наложены друг на друга, HTML-тег вставлен в `<text>`,
неразрывный токен добавлен в прозу): сработали **все четыре** детектора;
после перезагрузки страница снова чистая, зонд в DOM отсутствует. Развёртка
по ширинам проверена отдельно на заведомо сломанной странице в blob-URL —
дала непустой результат на всех четырёх ширинах.

Обе темы: 21 цветная подпись, ни одна не сливается с фоном ни в светлой, ни
в тёмной. Консоль чистая, рантайм-нумерация фигур и хлебные крошки собрались
из реестра, все якоря оглавления резолвятся.

---

## 2.2.1 — 2026-08-01 · пять коллизий геометрии и ловушка нулевого окна · AI Crypto

`d7e6f06`. Канонический прибор STYLE-GUIDE §7 зажигал пять коллизий, все
в двух главах части II. Сессия, писавшая эти главы, сдала их с `collisions: []` —
и это был ложно-отрицательный результат ловушки среды, а не чистая геометрия.

**Ловушка.** Сразу после подъёма предпросмотра `documentElement.clientWidth = 0`
и `innerWidth = 0`. В этом состоянии `getScreenCTM()` вырождается в масштаб
`a = d = 1`, метрики шрифта возвращаются целыми, и прибор молча отдаёт пустой
результат — неотличимо от пройденной приёмки. Мерить можно только после явного
`resize_window` **с повторной навигацией**, убедившись, что `clientWidth`
ненулевой и `document.fonts.status === 'loaded'`. Пустой результат без такой
проверки не доказывает ничего.

**Рис. 8.4, четыре коллизии.** `.viz-down` задаёт только цвет и насыщенность,
размера в нём нет. Поэтому `<text class="viz-down">` без атрибута наследовал
17px страницы, тогда как две строки над ним в той же рамке — 11px: подпись
«▼ меньше» рисовалась в полтора раза крупнее задуманного и налезала на строку
выше (перекрытие 3.07 при пороге 3). Все прочие `viz-down` в этой главе размер
задают (13/15/15/15/17) — эти четыре оказались единственными без него.
Переведены на `class="viz-axis viz-down"`: размер как у соседей по рамке, цвет
`--dir-down` и насыщенность сохранены, зазор стал 2.95 — норма книги для
соседних строк.

**Рис. 9.4, одна коллизия.** Между низом заголовка и верхом деления «20» было
32.95 единицы на две строки, которым нужно 30.10, — подзаголовок и подпись оси
«очередь» садились впритык (перекрытие 3.05). Места не хватало по построению,
поэтому поле графика опущено на 8 группой `translate(0,8)`, `viewBox` 420 → 428,
подпись оси на 56. Оба зазора стали 2.95. Данные не тронуты: отметки
по-прежнему дают 1 / 2.31 / 4 / 9 / 19.

Порог прибора не двигали и подписей не удаляли: чинилась вёрстка, а не мерка.

**Сверка `desc` с графикой в главе 9.** У `fcp3d` две ячейки таблицы стояли
крест-накрест — верхняя правая описана как «вынужденное накопление», хотя
по шапкам столбцов и строк это нижняя левая, а верхняя правая — «распродажа
склада». Экранный диктор получал таблицу зеркально неверной; ячейки возвращены
на места. У `fcp5d` числа сошлись все; поправлены «Справа три рамки» (две
из трёх идут во всю ширину внизу) и пропуск порога 50, названного в графике.
По правилу этого журнала точечные правки фактов идут в `errata.html` — здесь
они записаны потому, что неотделимы от починки вёрстки того же коммита.

---

## 2.2 — 2026-07-28 · часть II закрыта: энергия и цепочки поставок · AI Crypto

Две новые главы, последние в части «Предложение и рост». **Часть закрыта
целиком — четыре главы из четырёх.** Первые две говорили о медленных
множителях предложения — отдаче часа труда и числе самих часов; эти две —
о факторах предложения, которые успевают сработать внутри бизнес-цикла,
а не поверх него.

**Написано 36 глав из 71**, 309 748 слов прозы, 183 фигуры.

### Глава «Энергия как макро-переменная» — 11 715 слов, 7 фигур

Три канала, которыми цена энергии входит в макроэкономику, и вопрос, которого
в них нет; цена нефти как равновесие, а не как переменная, и проблема
идентификации в её чистейшем виде; **разложение нефтяного шока на предложение,
совокупный спрос и предупредительный спрос** и знаки последствий каждой
компоненты вплоть до противоположных для выпуска и для акций; четыре
наблюдаемых ежедневно признака различения компонент; спор об идентификации
и о том, был ли нефтяной шок причиной стагфляции; пять множителей, стоящих
между ценой энергии и выпуском, и направление изменения каждого за полвека;
три участка проброса в цены со своей скоростью и своей асимметрией; газ
и электроэнергия как отдельные объекты, у которых нет одного мирового рынка;
спрос дата-центров как шок спроса на энергию, а не предложения; базовая
инфляция и дерево из двух вопросов, отвечающее, когда исключение энергии
оправданно.

**Главный результат — отрицательный.** Правило шпаргалки «нефть
<span aria-hidden="true">▲</span> → состояние экономики лучше» верно ровно
в одной компоненте из трёх, нейтрально во второй и даёт обратный знак
в третьей — той, которая исторически ответственна за самые крупные движения
цены. Значит, у правила, читающего цену без разложения, **вероятность ошибки
знака растёт вместе с величиной движения**.

**Что глава добавляет к инструменту.** Ни одного веса и ни одного порога —
они в спецификации, §1.4. Три обоснования, ни одного решения: разность между
отношением меди к золоту и отношением нефти к золоту несёт информацию,
которой нет ни в одном из них, и просится флагом рядом с осью (решение —
спецификации, и вводить его без измерения нельзя); во время энергетического
шока обе оси перестают быть независимыми, потому что шок двигает одну,
а реакция на шок — другую; правила о нефти и о сырье нуждаются
в восстановлении потерянного при пересказе условия «при движении цены,
вызванном мировым спросом».

### Глава «Цепочки поставок и шоки предложения» — 10 763 слова, 7 фигур

Формальное определение шока предложения через совместный знак цены и объёма
и четыре причины, по которым это определение не работает на агрегате;
**арифметика эффекта хлыста**, посчитанная на бумаге: пятипроцентный шаг
конечного спроса даёт заводу скачок заказов на 40 %, а в следующем месяце —
падение на 20 % ниже исходного уровня, тогда как потребитель тратит на 5 %
больше прежнего; четыре причины усиления и пятая, найденная раньше остальных;
запасы как буфер и как усилитель, включая вклад изменения запасов в счёт
выпуска; нелинейность очередей — рост потока заявок на 12,5 % увеличивает
очередь в 2,25 раза; ковидная фрагментация и три свойства, собранные
в полупроводниках; устройство сводного индекса давления в цепочках и цена
шага «вычесть спрос»; сети и гранулярность; тарифы как сдвиг уровня против
фрагментации как сдвига устройства.

**Центральный узел — пять механизмов, из-за которых шок предложения читается
как перегрев.** Подындекс сроков поставки входит в композитный индекс деловой
активности напрямую и со знаком, при котором нехватка мощности **повышает**
показатель силы спроса; хлыст надувает заказы; нормирование при дефиците
наполняет книгу заказов фантомной составляющей; агрегат складывает секторы
с разными знаками; неблагоприятный шок предложения снижает оценку
потенциального выпуска, то есть двигает сам эталон. Пять механизмов
независимы и складываются в связную картину перегрева. Симметричная ошибка
на выходе из эпизода названа там же: заказы падают при неизменном конечном
спросе, и та же конструкция предсказывает спад, которого не происходит.

**Что глава добавляет к инструменту.** Ни одного веса и ни одного порога.
Три обоснования, ни одного решения: разность между темпом заказов и темпом
конечных продаж просится признаком достоверности блока заказов (флаг рядом
с осью, не слагаемое); у ближайшей задачи контура — построения открытого
заменителя проприетарного индекса деловой активности — есть непроговорённая
развилка, **включать ли в заменитель подындекс сроков поставки**; правила,
читающие рост портфеля заказов и удлинение сроков поставки как признак силы
спроса, нуждаются в записанном рядом условии «при отсутствии дефицита
мощности». Вес блока заказов глава трогать не предлагает: он выведен
из положения звена в цепочке опережения, и шок предложения этого основания
не отменяет.

### Что в этих главах не является измерением

Ни одной задачи контура `research/` по энергии и цепочкам поставок
не заведено, и обе главы говорят это первой строкой блока «Как это проверить».
**Пометок `[НАШ РАСЧЁТ]` в двух главах ноль** — считать было не на чем.
Пометок `[НЕ ПРОВЕРЕНО]` двадцать одна, каждая с указанием причины.
Отдельно названы ряды, без которых проверка невозможна и которых в описи
контура нет: мировая добыча нефти, мера мировой реальной активности, запасы
нефти, фьючерсная кривая, потребление электроэнергии, энергоёмкость ВВП,
фрахтовые индексы, портфель невыполненных заказов, межотраслевые таблицы
и сам сводный индекс давления в цепочках. Источник по энергетике требует
ключа, которого у контура нет. Глубина истории названа только там, где ряд
в описи есть, и взята из неё, а не по памяти.

**Что из этих утверждений всё-таки проверяемо сегодня** — сказано отдельно
и поимённо: разность двух сырьевых отношений, асимметрия проброса
в розничные цены, реакция мер ожидаемой инфляции на нефть, рост амплитуды
заказов по мере удаления от конечного спроса, соотношение изменчивости
производства и продаж, поведение цен импорта вокруг тарифного эпизода.

### Правка вёрстки

Правок общих файлов нет: `book.css` и `book.js` не тронуты. Замер геометрии
в браузере на ширинах 320–1600 px нашёл в новых главах три места, где подпись
выходила за границу своей рамки внутри графики (20 px в главе об энергии,
2 и 13 px в главе о цепочках); все три исправлены расширением рамки
и перестановкой подписей осей за пределы ячеек. Горизонтального скролла
страницы нет ни на одной из двенадцати проверенных ширин.

---

## 2.1 — 2026-07-28 · часть II открыта: производительность и капитал, демография и предложение труда · AI Crypto

Две новые главы, первые в части «Предложение и рост». До них книга говорила
о спросе — сколько экономика тратит и как это колеблется; часть II начинает
разговор о потолке, вокруг которого спрос колеблется.

**Написано 34 главы из 71**, 287 270 слов прозы, 169 фигур.

### Глава «Откуда берётся рост: производительность и капитал» — 11 224 слова, 8 фигур

Разложение роста Солоу и остаток, названный производительностью; накопление
капитала, углубление против расширения и убывающая отдача; человеческий
капитал и эффект состава; шесть слоёв, из которых состоит остаток; почему
ряды производительности не читаются квартально; загадка замедления 2005–2019
годов с картой разводящих наблюдений для четырёх объяснений; всплеск 2020-х
и процедура, различающая возврат к тренду и новый тренд; капекс на ИИ,
разложенный на три разных вопроса — спрос сегодня, потенциал завтра,
концентрация индекса; акселератор как механическое основание цепочки
опережения; рабочая таблица различения ускорения спроса и роста потенциала;
четыре канала, по которым производительность доходит до цен активов.

**Что глава добавляет к инструменту.** Ни одного веса и ни одного порога —
они в спецификации, §1.4. Три требования: ось MACRO по построению не различает
ускорение спроса и рост потенциала, и различающий признак (затраты на единицу
труда) может войти только отдельным флагом, а не слагаемым; капекс на ИИ —
случай, ради которого существует поправка на концентрацию (§1.4.8);
ненаблюдаемые величины в пороге правила не стоят.

### Глава «Демография и предложение труда» — 10 097 слов, 7 фигур

Тождество предложения труда и шесть переходов между тремя состояниями;
инерционность демографии и граница её предсказуемости (рождаемость и миграция);
участие как смесь трёх процессов и очищенная мера по группе 25–54 лет;
иммиграция как единственный быстрый рычаг предложения труда; точка
безубыточности по занятости, её арифметика и дрейф; **полный разбор ложного
срабатывания правила Сама в 2024 году**; демография и равновесная ставка
со спором о знаке; демография как фискальная переменная; Япония и Европа
как опережающие примеры.

**Погашено обещание.** Глава о труде фиксировала осечку правила Сама одной
фразой, глава о формах кривой — одной строкой без объяснения; решение
о включении правила в инструмент выносилось в главу о переводе шпаргалки
в код и там не принималось. Здесь эпизод разобран до механизма (одна и та же
цифра получена расширением знаменателя, а не разрушением рабочих мест),
и решение принято: **правило Сама в свёртку не входит** — оно построено
на показателе-запасе, а запасы рынка труда имеют нулевой вес по конструкции
оси. Остаётся наблюдением рядом с осью и читается только с признаком
механизма. Эпизод 2024 года попутно подтвердил уже принятое решение о ненулевом
весе первичных заявок: запасы дали сигнал, поток не дал, правым оказался поток.

### Что в этих главах не является измерением

Ни одна задача контура `research/` по производительности, капиталу
и демографии не заведена, и главы это говорят прямо. Пометок `[НАШ РАСЧЁТ]`
семь на две главы, все со ссылкой на выполненные задачи `Z02` (порядок звеньев
цепочки опережения) и `Z03` (частота ложных тревог сигнала инверсии).
Пометок `[НЕ ПРОВЕРЕНО]` девятнадцать — по одной на каждое утверждение,
которого мы не считали, с указанием причины. Ряды производительности, затрат
на единицу труда, компонент инвестиций, населения по возрастам, участия
по группам, разбивки безработных по причине незанятости и миграции
**в описи контура отсутствуют**, поэтому глубина их истории в книге
не называется: правило проекта запрещает писать даты начала истории
по памяти.

### Правка вёрстки, найденная замером

`assets/book.css`: `overflow-wrap: break-word` распространён с абзацев
на заголовки `.prose h1/h2/h3`. Длинное русское слово в заголовке при кегле
`h1` не влезает в 320 px и выталкивает страницу в горизонтальный скролл —
поймано замером геометрии на главе о производительности, переполнение 15 px.
Правило общее, а не поштучное, по той же причине, что и для абзацев: заголовок
есть в каждой главе, а длинных слов в русских названиях впереди ещё много.
Прогон на десяти уже сданных страницах и на обложке показал, что правка
их вёрстку не меняет.

---

## 2.0.2 — 2026-07-28 · структура курса проверена на данных: `Z02`, `Z03`, `Z04` и дополнение `Z01` · AI Crypto

Патч-редакция по итогам трёх новых задач контура `research/` и дополнения
к первой. Счёт вердиктов контура стал таким: **выполнено 4 задачи из 24,
два опровержения (`Z01` — число лида, `Z02` — порядок звеньев) и два
неопределённых (`Z03`, `Z04`). Ни одна задача не подтвердила число курса
как есть.**

Содержания глав правка не добавляет и не убирает: механизмы, разобранные
в книге, проверку пережили целиком. Меняется то, что книга **обещает
читателю как измеренное**.

### Самое срочное — то, что книга печатала опровергнутым

Подпись рис. 51.3 в главе «Структурные разрывы и режимы» утверждала, что
в первой половине истории связь «почти вдвое сильнее» — −0,637 против −0,373.
**Это неверно.** Разница целиком ковидная: после исключения 2020-03 … 2021-12
тем же правилом, каким это делает сама задача, поздняя половина даёт
**−0,647 против −0,637** у ранней, то есть чуть *сильнее*. Правка сквозная —
подпись, `desc`, третья кривая на фигуре, третья колонка в табличном двойнике
и разбор под фигурой. Два других различия двух половин устояли, и раздел
снятию не подлежит.

### `Z02` — порядок H-O-P-E ОПРОВЕРГНУТ, двенадцать ступеней остаются

**Что проверялось.** «Экономика разворачивается в порядке NAHB → Permits →
ISM New Orders → Confidence → Consumer New Orders → Manufacturing Sales →
Cap Goods → Industrial Production → AWH → Payrolls → Personal Income →
Core CPI, и этот порядок устойчив от цикла к циклу» — правило `R77`, на
котором стоит вся горизонтальная ось инструмента.

**Что получилось.** Объявленный до расчёта порог рангового согласия `ρ > 0.6`
**не взят ни в одном из шести циклов** (медиана 0,29, максимум 0,57), а по всем
139 конфигурациям расчёта — в двух ячейках. Оба 90 %-х блочных бутстрап-интервала
на полной выборке накрывают ноль. Лучший `p` после Холма — 0,18.

**Решающий довод — не сравнение с книгой, а сравнение циклов между собой.**
Если у цепочки есть порядок, циклы обязаны согласовываться друг с другом
сильнее, чем каждый из них — с чужой гипотезой. Вышло наоборот: медиана
попарного согласия шести циклов **+0,18** против **+0,29** согласия с книгой.
Транзитивность «раньше» нарушена в 18–39 % троек; у 24 пар звеньев из 66
измеренный лид не превышает месяца. Порядок не «другой, чем в книге» —
устойчивого порядка двенадцати звеньев в данных нет вообще.

**Что устояло, и это говорится так же громко:** жильё первым (Permits и NAHB —
два самых ранних медианных ранга), Core CPI последним (единственное звено,
у которого и медианный ранг, и знак лида во всех трёх панелях совпали
с картой), общий градиент, `H` раньше `O` в четырёх циклах из шести.
**Вес жилья `0.26` в §1.4.4 спецификации основание сохраняет и не меняется.
Ни одно число §1.4 не изменилось.**

**Форма правки — решение оператора, записанное в журнал `PROGRESS.md`
2026-07-27 и выбранное из трёх вариантов.** Опровергнутая структура курса
**не выбрасывается**: двенадцать ступеней остаются как изложение курса, рядом
ставится то, что от порядка осталось после проверки — **три этажа без
внутреннего порядка**: раннее (жильё, новые заказы), среднее (производство,
продажи, капитальные товары, часы, занятость), позднее (цены). Отклонены
и заново не предлагаются варианты «заменить тремя блоками» и «оставить
порядок с одной лишь пометкой». Задания «выучить порядок» переформулированы:
учить надо не порядок, а то, почему он неустойчив.

**Отдельная находка, ради которой стоило считать.** Пятизвенная схема
`H → O → P → E → I` даёт согласие **0,90 и 0,97** в циклах 2001 и 2007 годов,
вызванных ставкой и кредитом, и **0,00 и −0,15** в нефтяных циклах 1980
и 1981 годов, где жильё разворачивалось **последним**. Оговорка курса о том,
что карта описывает отклик на ужесточение ФРС, а не любой спад, была помечена
в книге как наше рассуждение `[ВЫВЕДЕНО]`; расчёт превратил её в измеренный
факт и в главное ограничение схемы.

### `Z03` и `Z04` — оба НЕОПРЕДЕЛЕНО, но с измеренными числами

**Впервые в книге посчитана частота ложных тревог.** `R29` (инверсия кривой) —
**первое правило спецификации из девяноста пяти, у которого она измерена**;
до этого частота ложных срабатываний называлась проблемой пять раз в пяти
главах и не была измерена ни у одного правила.

- Основная конфигурация: **4 верных срабатывания, 2 ложные тревоги,
  0 пропусков** (`FA/TP` = 0,50 — ровно на объявленной границе).
- При окне ожидания 12 месяцев: `FA/TP` = **2,00**.
- По сетке из 24 определений: **66 верных против 90 ложных**,
  медиана `FA/TP` = **1,67**, размах 0,20 … 6,00.
- Лаг — интервал, а не число: медиана **13 месяцев**, размах **8–16**;
  по объединённой сетке — **4–24**.

**Структура ответа важнее чисел.** При окне ожидания 12 месяцев не проходит
**ни одна ячейка из двенадцати**, при 24 месяцах — **шесть**. Утверждение
не разваливается и не подтверждается: оно распадается на определения. Отсюда
требование к спецификации — **окно ожидания обязано быть частью правила**,
иначе `R29` неисполнимо. Численного веса в свёртке правило не получает.
Эпизод 2022–2024 годов — ложная тревога **во всех 24 ячейках**; формулировка
врезки «2023 → 2026» в главе о формах кривой поэтому **усилена, а не смягчена**.
Различие AUC двух спредов вне выборки **не установлено**.

**`Z04`: парных эпизодов два, а не семь.** При `n` = 2 объявленный критерий
срабатывает вхолостую **в половине случаев**. На самой длинной доступной
выборке точечная оценка смотрит против утверждения: разброс лага у дезинверсии
*больше*, а не меньше. **Три лага дезинверсии из четырёх отрицательны** —
рецессия уже шла, когда кривая выпрямилась. Механизм прямой: кривая
выпрямляется, потому что ФРС начинает снижать ставку, а снижать она начинает,
когда спад уже виден. Пункт 6(б) карты пробелов называл дезинверсию
«исторически более точным» сигналом — **оснований нет, строка переписана**:
дезинверсия вводится как отдельное описываемое событие, но не как более точный
сигнал. Везде, где называется её дата, теперь назван ряд: по `T10Y2Y` это
2024-09, по `T10Y3M` — 2024-12.

**Реал-тайм.** Комитет NBER объявляет пик через 4–12 месяцев, вердикт
по эпизоду становится известен через 13–36; механическое правило двух кварталов
**медленнее** Комитета (1990 — 26 месяцев против 9, 2001 — 20 против 8;
для 1980 года пара кварталов появилась только в винтаже 1996-го). И **винтаж
меняет ответ не меньше определения**: в редакции августа 2022 года два квартала
подряд шли со спадом (−0,40 % и −0,23 %), сегодня второй даёт **+0,16 %**.
Проверено независимо через архив ревизий ALFRED.

**Превосходная степень проверена и распалась надвое.** Инверсия 2022–2024 годов
самая длинная при строгом прочтении слова «непрерывная» (**537 подряд идущих
торговых дней**, 2022-07-06 … 2024-08-26) и **не** самая длинная при мягком:
с допуском разрывов до трёх месяцев эпизод 1978–1982 годов даёт 45 месяцев
против 28. Разница между прочтениями — полтора года.

### Дополнение `Z01`

- **Раздел о двух половинах устоял:** поздняя половина без ковида даёт максимум
  на **10** месяцах, а не на 19. Метка `[НЕ ПРОВЕРЕНО]` в утверждении 2 главы
  о структурных разрывах снята, упражнение «которого мы не сделали»
  переформулировано — оно сделано, и следующий незакрытый вопрос назван.
- **«Пик на девятом месяце» — не устойчивая черта:** бутстрап двугорбый,
  медиана лага 22, интервал [9, 28], в полосу 8–12 попадает 35,5 % выборок.
  Выигрыш аргмакса — на третьем знаке.
- **Независимых пар разворотов шесть, а не семь** (1989-03 общий для пиков
  1986-06 и 1988-12). Медиана 16 устояла, размах сократился до 3–29.
  Во второй таблице событий 10 вместо 13, центр уехал с 15 к **12,5**.
- **Поправка Холма:** честный размер семейства — 16, и поправка даёт **0,048**
  при пороге 0,05; при 17 конфигурациях — 0,051. Значимость решается размером
  объявленного семейства, а не данными. Глава о проверке вне выборки написана
  уже на этом материале и правки не потребовала; приведена в соответствие
  более ранняя глава о проверке макро-утверждения и спецификация, которая
  несла устаревшее «Холм — 0,006».

### Где правилось

Порядок правки прежний: сначала спецификация (главы ссылаются на неё,
а не наоборот), затем каталог индикаторов и карта пробелов, затем главы,
затем `errata.html` и этот журнал.

- `../synthesis/cheatsheet-spec.md` — §1.2 (порядок опережения), §1.4.2,
  §1.4.4, §1.4.5, §7-B (`R15` — шесть пар и пересчитанная поправка Холма),
  §7-D (`R29` — измеренная частота ложных срабатываний и отдельная врезка
  по дезинверсии), §7-K (`R77`), §8.9 (дыра сузилась на одно правило),
  плюс запись в журнале пересмотров §10. **Правило `R29` отмечено там
  как первое правило спецификации с измеренной частотой ложных
  срабатываний.**
- `../synthesis/indicator-catalog.md` — врезка в §0.2 и двенадцать карточек,
  где стояла «позиция №N по карте Piper Sandler», плюс легенда сводной
  таблицы. Собственный обход нашёл двенадцать мест вместо семи, названных
  отчётом.
- `../synthesis/coverage-and-gaps.md` — §5 п. 6 (оба подпункта), строка
  «Инверсия → рецессия» в таблице пробелов курса, §4.1 п. 7 (асимметрия
  цепочки «производство против прибылей») и статус пробела §G п. 20.
- Главы: структурные разрывы (сквозная правка фигуры и двух разделов),
  H-O-P-E (сквозная, включая новый раздел «Как это проверить»), жильё,
  заказы, прибыль, труд и потребитель, цены и инфляция, сентимент,
  эволюция шпаргалки, анатомия шпаргалки, от шпаргалки к коду, что такое
  макроэкономика, бизнес-цикл, формы и реальные ставки, как читать
  эмпирику, что значит проверить макро-утверждение, лид-лаг, реал-тайм,
  framework.
- `index.html` — номер редакции.

Главы части XII, написанные уже на материале этих задач — «Проверка вне
выборки и множественность гипотез» и «Реал-тайм против пересмотренного», —
формулировок результата не меняли: они и служили эталоном для всех остальных
мест, второй расходящейся версии в книге нет. В главе о реал-тайме добавлены
только квартальные числа винтажного довода.

Парная запись с полным «что было → что стало» — в [errata.html](errata.html).

---

## 2.0.1 — 2026-07-27 · первое число курса проверено на данных и заменено интервалом · AI Crypto

Патч-редакция по итогам первой доведённой до вердикта задачи контура
`research/` — **`Z01`**, проверка обоих чисел правила `R15`.

**Что проверялось.** «NAHB опережает уровень безработицы на 10 месяцев»
и «NAHB год-к-году опережает доходность десятилетних год-к-году на 18 месяцев».
Критерии, полоса допуска и состав конфигураций записаны **до расчёта**
(`../research/Z01/HYPOTHESIS.md`); результат воспроизведён независимо — другим
кодом и другой загрузкой рядов. Ряды: NAHB HMI с 1985-01 (сайт публикатора),
`UNRATE` и `DGS10` (FRED). Преобразование — годовые приращения,
сбалансированная панель `n` = 441.

**Что получилось.**

- **Лид к безработице — ОПРОВЕРГНУТО как число.** Максимум взаимной корреляции
  лежит на **17 месяцах** (без ковидных лет — 19), а не на 10. В объявленную
  заранее полосу 8–12 месяцев попало 7 % скользящих десятилетних окон
  и 16.7 % бутстрап-выборок. Независимый счёт по разворотам даёт то же:
  семь пар «пик жилья → минимум безработицы», медиана 16, размах 3–33.
  *(Исправлено в 2.0.2: независимых событий **шесть**, а не семь — две пары
  ведут к одному минимуму безработицы; медиана 16 устояла, размах 3–29.)*
- **Сама связь — ПОДТВЕРЖДЕНА и сильна.** Знак верен во **всех 331** окне
  без единого исключения, `p` = 0,003 даже с поправкой на перебор 37 сдвигов.
- **Лид к доходности — НЕОПРЕДЕЛЕНО.** Максимум ровно на 18 месяцах, но
  `p` = 0,135 после поправки на поиск лага, интервал лага [9, 36], вне выборки
  связь оседает до +0,08 против +0,30 на обучающем куске.

**Как это НЕ читать.** Правило `R15` **не отменяется**, и жильё **не теряет**
статус самого раннего звена. Опровергнуто число, а не механизм — и опровергнуто
в сторону, для правила выгодную: жильё опережает рынок труда *сильнее*, чем
говорит курс. Предрегистрация при вердикте «опровергнуто» требовала снять
формулировку правила и пересмотреть вес жилья `0.26` в §1.4.4 спецификации.
Оба требования исполнены, и оба закончились сохранением: формулировка заменена
(точка → интервал), вес оставлен прежним — он выводится из порядка `R77`,
а проверка этот порядок укрепила, а не поколебала.

**Форма замены — прецедент для всех будущих задач.** Опровергнутое число
заменяется **не другим числом, а интервалом** с пометкой **[НАШ РАСЧЁТ]**
и ссылкой на папку задачи: *«около полутора лет; точка максимума 17 месяцев,
полоса 8–22»*. Причина названа в главе «Как читать эмпирическое утверждение»:
на участке 8–22 месяца корреляция держится между −0,33 и −0,42 — кривая почти
плоская, и «17» была бы такой же точечной оценкой, как «10», просто нашей.

**Где правилось.** Порядок правки: сначала спецификация (главы ссылаются
на неё, а не наоборот), затем каталог индикаторов, затем главы. Объём первого
коммита волны (`63f77ac`) — **15 файлов, 40 правок в восьми главах** по счёту
`git show -U0`; предварительная оценка отчёта `Z01` §6 («в двадцати с лишним
местах, из них девять — в одной главе») занижена: обход нашёл на две главы
больше и вдвое больше мест. Доводочный коммит по трём рецензиям добавил
девятую главу и три документа вне `book/`.

- `../synthesis/cheatsheet-spec.md` — сводка лидов §1.2 и формулировка `R15`
  §7-B, плюс две врезки `[ПЕРЕСМОТРЕНО]` и запись в журнале §10.
- `../synthesis/indicator-catalog.md` — карточки 1.1 и 3.4, §7.7, сводная
  таблица разворотов, оговорка §G.
- `../synthesis/coverage-and-gaps.md` — статус пробела «валидация правил».
- Главы (счёт правок по `63f77ac`, сорок на восемь глав): жильё (девятнадцать — обе фигуры
  целиком, их `desc`, наводки и табличные двойники, плюс новый блок
  «Как это проверить»), H-O-P-E (5), бизнес-цикл (6), источники данных (3),
  труд и потребитель (2), межрыночный анализ (2), анатомия шпаргалки (2),
  эволюция шпаргалки (1).
- `STYLE-GUIDE.md` — образец карточки индикатора: устаревшее число
  в образце копировалось бы в каждую новую главу.
- `index.html` — номер редакции.
- Доводкой по рецензиям: `../BOOK-PLAN.md` (§3 план главы о жилье и §8),
  `../VOLUME-I-TEXTBOOK-AUDIT.md` и `../CROSS-READ-REPORT.md` — врезки-статусы
  в реестрах непроверенных утверждений: после `Z01` оба реестра числили
  этот пункт непроверенным.

Глава «Как читать эмпирическое утверждение» формулировок результата
не меняла: она написана на материале `Z01`, и её текст служил эталоном
для всех остальных мест — второй, расходящейся формулировки того же
результата в книге нет. Доводкой в ней поправлено одно предложение:
оно говорило, что правка книги ещё предстоит, и после волны стало ложным.

Парная запись с полным «что было → что стало» — в [errata.html](errata.html).

---

## 2.0 — 2026-07-27 · номер главы вычисляется, а не хранится · AI Crypto

Структурная редакция. Содержание глав не менялось — менялось то, чем глава
опознаётся. Повод: книга растёт с 24 глав до 71, из них **47 вставляются
в середину**. При прежней схеме каждая вставка ломала ссылки, поэтому схему
чинили до того, как написана хоть одна новая глава.

**Главное изменение — номер главы больше не хранится.** Он равен позиции главы
в `assets/chapters.js` плюс один. Настоящий идентификатор главы — устойчивый
слаг (`hope`, `dengi`, `r-star`), который не меняется никогда. Вставка главы
в середину книги теперь стоит **одну строку в реестре**.

Что из этого следует:

- **Файлы глав потеряли числовой префикс**: `hope.html`, `dengi-i-kredit.html`.
  Переименование сделано через `git mv` — история файлов цела.
- **В разметке `<body data-chapter-id="слаг">`** вместо `data-chapter="N"`.
- **Перекрёстные отсылки стали символическими.** 271 отсылка к главе и 58 к
  фигуре: автор пишет слаг и падеж, число подставляет `book.js`. Записанное
  в разметке число остаётся запасным вариантом для печати и для чтения без
  JavaScript; `tools/linkify.py --check` следит, чтобы оно не разошлось
  с вычисленным, а `--fix` пересчитывает его по реестру.
- **Id фигур переведены на слаги** (`fig-hope-2`), номер «N.k» рисуется из
  позиции главы. Хлебная крошка и заголовок вкладки — тоже из реестра.

**Структура книги.** Реестр содержит все 71 позицию в 17 частях: 24 написанные
и 47 запланированных со `status: "todo"`. Оглавление и боковая панель сразу
показывают карту всей книги; ненаписанные главы видны, но не кликаются.
Часть II первой редакции («Двигатель цикла») стала частью IV, синтез — частью
XIV, и так далее; полная таблица частей — `../FULL-BOOK-PLAN.md` §2.

**Глава «Деньги, кредит и ликвидность» переехала** с позиции 4 на 21, вплотную
к части о центральном банке: она объясняет инструменты ФРС, а не ВВП. Входящих
ссылок из глав, стоявших после неё, у неё не было — проверено.

**Починен унаследованный дефект.** В главах 15, 16 и 69 номер фигуры не совпадал
с порядком чтения: за первой шла третья, потом вторая. При статической подписи
это было незаметно; при вычисляемой стало ошибкой. Порядок исправлен, отсылки
перевешены. Всего у 20 глав из 24 сменился номер, и вместе с ними — номера
всех 105 фигур.

**Инструменты.** `tools/linkify.py` переписан: теперь это не разовый
проставлятель ссылок, а инструмент сопровождения (`--check` / `--fix`).
`tools/audit.py` получил три новых дефекта (слаг вне реестра, разошедшееся
число, текстовая «глава N» вместо отсылки), два новых класса врезок
(`callout--test`, `callout--debate`) и три новые пометки происхождения
(`[СПОР]`, `[НАШ РАСЧЁТ]`, `[НЕ ПРОВЕРЕНО]`). Сверку отсылок аудит зовёт
из `linkify`: две реализации одной сверки — ровно тот класс ошибки, ради
которого затевалась эта редакция. Эталоны `tools/fixtures/*` дополнены,
`--selftest` зелёный.

**Правила письма.** Крипто-слой и врезка «2023 → 2026» перестали быть
обязательными: обязательность превратила их в рефрен. Добавлен блок
«Как это проверить» — обязательный для главы, делающей эмпирическое
утверждение. Подробно — `STYLE-GUIDE.md` §1.1 и §3.

Проверка редакции: `tools/audit.py` — ноль дефектов, 24 главы на месте;
`tools/linkify.py --check` — ноль битых ссылок и якорей; геометрия
320–1600 px; обе темы.

---

## 1.1 — 2026-07-27 · единственное место истины для весов и порогов · AI Crypto

Правка по итогам сквозной диагностики (`../CROSS-READ-REPORT.md`). Диагностика
нашла четыре блокирующих расхождения между главами, и все четыре имели одну
причину: **веса, пороги и схема агрегации предлагались независимо в пяти главах**.
Заплатки повторили бы это при следующей правке, поэтому чинилась причина.

**Главное изменение — правило, по которому пишутся главы.** Числа свёртки
(веса входов обеих осей, пороги `θ`, окна, шкала ISM) вынесены в единственное
место — **§1.4 `synthesis/cheatsheet-spec.md`** «Схема агрегации осей». Главы 5,
7, 14, 20, 21 и 24 объясняют механизм и ссылаются туда; собственных таблиц
значений они больше не заводят. Из главы 24 удалены две таблицы весов.

Что разрешено содержательно:

- **Двойной счёт LEI.** Глава 20 запрещала ставить в ось MACRO композит вместе
  с его компонентами, глава 24 ставила и то и другое. Развилка закрыта в пользу
  **компонент**: `LEI YoY` в свёртку не входит, `R12` сохраняет номер и работает
  независимой сверкой знака. Проведено через главы 5, 20, 24 и §1.2/§1.4.5/§8.12
  спецификации.
- **Перевёрнутые веса LEI и прибыли.** Глава 5 клала LEI в ярус входа, а EPS —
  в подтверждающий; глава 24 давала ровно обратное. Разрешено в пользу главы 5.
  Там же снята инверсия «O весит больше H» при выводе весов из `R77`.
- **Поправка на концентрацию индекса.** Обещана главами 8 и 21, в главе 24
  отсутствовала полностью. Написан раздел `§s61`: вход прибыли считается на
  капвзвешенной и равновзвешенной версиях (в ось идёт худшая), спред `RSP/SPY`
  против своей средней понижает вклад `R64` и `R68`. Новых номеров `R##` не
  выдавалось — нумерация `R01`–`R95` заморожена.
- **Схлопнутая шкала ISM.** Глава 24 сводила четыре уровня главы 7 к «против 50»
  и равным половинам `0.5/0.5`. Приведено в соответствие с `R09`/`R10`:
  `c = 0.6·sign(Δ₃м) + 0.4·L`, где `L` — четырёхступенчатое уровневое слагаемое.
  Неравные доли делают правило «при конфликте побеждает направление» арифметикой,
  а не отдельной веткой в коде.
- **Окно валидатора кривой.** 30 календарных дней вместо 21 торгового —
  одно значение по всей книге; способ агрегации трёх окон — голосование
  большинством, а не иерархия.
- **Ось FED: шесть входов или девять.** Все девять входов §1.1 теперь выписаны
  явно, два весят ноль по названной причине, `R07` работает переопределением
  знака, а не слагаемым.

Фактические и структурные починки:

- **«88 правил в тринадцати группах» → «95 в четырнадцати»** — глава 24
  (заголовок, оглавление, `meta`, рис. 24.3 с подписью и табличным двойником),
  `index.html`, глава 21, рис. 1.4 главы 1, `BOOK-PLAN.md`, `STYLE-GUIDE.md`.
  Блок N (`R89`–`R95`) добавлен в каталог 2026-07-27.
- **Глава 1 получила обязательный блок «Что читать дальше»** — она была
  единственной из 24 без него, потому что написана до появления требования.
- **`R33`, `R34`, `R39`, `R54` названы по номеру** в главе 20: раньше они жили
  в книге только внутри диапазонов, хотя используются по смыслу.
- **Крипто-калибровка −48.66 %** в семнадцати главах приводилась без оговорки
  главы 22 о её непереносимости. Оговорка и ссылка добавлены к каждому вхождению;
  развёрнутый разбор остаётся в главе 22.
- **206 текстовых «см. главу N» превращены в гиперссылки** — 217 ссылок,
  проставленных новым `tools/linkify.py` по реестру `assets/chapters.js`.
  До этой правки на всю книгу была **одна** ссылка между главами.

**Новый инструмент:** `tools/linkify.py` — разметка отсылок и режим `--check`,
проверяющий, что ни одна ссылка книги (включая якоря на разделы) не ведёт
в никуда. На момент выпуска — ноль битых.

**Объём по замеру `audit.py`:** 24 главы, 167 756 слов прозы (1 130 228 знаков),
плюс 16 381 слово подписей внутри графики; 105 фигур, 152 таблицы,
82 карточки индикаторов, 397 врезок, 1 043 пометки происхождения.
Технических дефектов — ноль. Глава 1 перестала числиться «с дефектами».

---

## 1.0 — 2026-07-27 · инфраструктура сопровождения · AI Crypto

Книга объявлена завершённой редакцией. Содержание глав не менялось; добавлено
то, без чего 24 главы нельзя поддерживать дальше.

- **`tools/audit.py`** — аудит книги без внешних зависимостей. По каждой главе:
  объём, состав, пометки происхождения, обязательные блоки структуры
  (STYLE-GUIDE §3) и шесть классов технических дефектов (STYLE-GUIDE §5).
  Карта «класс → свойство» для проверки перебитых атрибутов читается из
  `assets/book.css`, а не зашита в код.
- **`tools/fixtures/{broken,clean}.html`** + режим `--selftest` — эталоны, на
  которых проверяются сами детекторы. Без них таблица из одних нулей
  неотличима от сломанного детектора.
- **`CHANGELOG.md`** — этот файл.
- **`errata.html`** — страница исправлений. В `assets/chapters.js` намеренно не
  входит: это не глава, и в навигации книги ей места нет.
- **Маркер редакции на обложке** — версия, дата, объём, ссылки на журнал и
  errata. Единственная новая группа правил в `book.css` — `.colophon`.

**Объём по замеру `audit.py`:** 24 главы, 164 922 слова прозы (1 111 697 знаков),
плюс 16 366 слов подписей внутри графики; 105 фигур, 154 таблицы,
82 карточки индикаторов, 393 врезки, 1 039 пометок происхождения.
Технических дефектов — ноль.

> Расхождение со старой цифрой «171 680 слов» — не потеря текста. Прежний
> замер снимался наивным вырезанием тегов регуляркой: у фигур есть подсказки
> `data-tip` с целыми абзацами внутри атрибута, и такой замер захватывал их
> вместе с прозой. `audit.py` считает текст `<article class="prose">` без SVG и
> без атрибутов, и печатает подписи графики отдельной колонкой.

---

## 0.9.2 — 2026-07-27 · 229 перебитых презентационных атрибутов · AI Crypto

`5945ab3`. Акцентные цвета в SVG не отрисовывались: CSS класса сильнее
презентационного атрибута, поэтому `<text class="viz-title" fill="var(--series-1)">`
оставался цвета `--ink`, а `<rect class="viz-box" stroke="var(--series-2)">` —
цвета `--hairline`. Ошибка ничем себя не проявляла: графика выглядела
законченной, просто монохромной там, где задумывался акцент. 229 мест
переведены на инлайн-стиль; механика описана в STYLE-GUIDE §5 п. 6.

С этой редакции дефект ловится автоматически — колонка «Атрибут под классом»
в `tools/audit.py`.

---

## 0.9.1 — 2026-07-27 · чек-лист без flex · AI Crypto

`f447c35`. `.checklist li` был flex-контейнером, и каждый вложенный тег пункта
(`.rule-id`, `.ticker`, `<b>`, `<code>`) становился отдельным flex-элементом:
текст рвался на колонки с зазором `gap`, а `min-width: auto` не давал пункту
сжаться — на 390 px страница получала горизонтальный скролл. Маркер `□`
переведён на `::before` с абсолютным позиционированием, пункт снова обычный
блок. Глава 1 ловушку не показывала: её чек-лист — чистый текст.

Обёртки `<span>` внутри пунктов глав 12, 16 и 17 — след того обхода. Они
безвредны, новые заводить не нужно (STYLE-GUIDE §4).

---

## 0.9 — 2026-07-27 · все 24 главы написаны · AI Crypto

Книга доведена от «что такое рыночный цикл» до технического задания на
классификатор. Главы писались параллельными сессиями и не по порядку;
ниже — порядок коммитов.

| Коммит | Глава |
|---|---|
| `5df6f63`, `f4d5678` | 2. Экономика как поток: ВВП *(+ приведение к §6)* |
| `33331a3`, `e9aa0f4` | 3. Бизнес-цикл и иерархия циклов *(+ приведение к §6)* |
| `a6e86be` | 5. H-O-P-E: скелет опережения |
| `ad74cf8` | 6. Жильё — первое звено |
| `1b52f31` | 10. Цены и инфляция |
| `c1115af` | 8. Прибыль корпораций |
| `a24df9d` | 4. Деньги, кредит и ликвидность |
| `1a675a1` | 14. Формы, режимы и реальные ставки |
| `a9af9e3` | 7. Заказы, производство, запасы |
| `f3b4043` | 13. Кривая доходности: механика |
| `842d010` | 19. Эволюция шпаргалки |
| `68b2628` | 22. Крипта как макро-актив |
| `4c1d397` | 11. ФРС: устройство и каналы |
| `f8b2553` | 16. Доллар, золото, сырьё |
| `68f9def`, `a68d418` | 20. Анатомия шпаргалки *(+ две находки в BOOK-PLAN §8)* |
| `75b56d7` | 23. Источники данных и гигиена |
| `1372f86` | 9. Труд, доходы и потребитель |
| `1c332e1` | 21. Framework макро-трейдера |
| `27c98e3` | 17. Факторы и факторная ротация |
| `0cf0ded` | 15. Межрыночный анализ |
| `3889f6a` | 24. От шпаргалки к коду |
| `b551122` | 12. Как читать ФРС |
| `0afb837` | 18. Сентимент, широта, позиционирование |

Попутно в `BOOK-PLAN.md` §8 накоплен разбор ошибок и противоречий курса:
тавтология правила R31, двойной счёт LEI, два противоречия по устройству ФРС,
две ошибки в главе о долларе и золоте, пять противоречий по факторам.

---

## 0.2 — 2026-07-27 · курс — каркас, а не потолок · AI Crypto

`c439ef0`. Смена правил работы с источниками (STYLE-GUIDE §6). До этого главы
реконструировали лекции 2023 года и только их. Стало: дополнять внешним
каноном — требование к каждой главе, а не «если останется место». Введена
дисциплина происхождения из пяти пометок — **[КАНОН]**, **[ВЫВЕДЕНО]**,
**[ДОПОЛНЕНО]**, **[НАШЕ РЕШЕНИЕ]**, **[НЕТ В КУРСЕ]** — чтобы читатель в
любой момент понимал, чей перед ним голос. Добавлены три запрета: не выдумывать
числа, не смешивать голоса, не подменять курс. Написанные к тому моменту главы
2 и 3 приведены к новым правилам отдельными коммитами.

Правило задало тон всей остальной книге: 1 039 пометок происхождения в
финальной редакции — из них 341 **[ДОПОЛНЕНО]** и всего 15 **[НЕТ В КУРСЕ]**.

---

## 0.1 — 2026-07-27 · разбор курса, план и глава 1 · AI Crypto

`6247b3b`. Основание книги:

- послайдовые конспекты лекций 3–11 (361 слайд) в `source-digests/`;
- синтез в `synthesis/`: спецификация шпаргалки с осями, квадрантами и
  правилами `R01–R88`, каталог индикаторов, разбор покрытия и пробелов;
- `BOOK-PLAN.md` — структура книги, порядок работы, разрешённые противоречия
  источника;
- HTML-каркас: `book.css` с валидированной палитрой, `book.js` (тема,
  навигация, тултипы, табличные двойники), `chapters.js` как единый реестр,
  `STYLE-GUIDE.md`;
- глава 1 «Карта вместо прогноза» — эталон вёрстки для всех последующих.
